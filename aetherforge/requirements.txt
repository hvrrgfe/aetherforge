# AetherForge - Core Requirements
flask>=3.0
mcp>=1.0
pyyaml>=6.0
Pillow>=10.0
numpy>=1.24

# Optional extras (install as needed):
# pip install pymunk    -> Physics
# pip install pygame    -> Audio
# pip install torch diffusers transformers  -> AI Image Gen
# pip install audiocraft  -> AI Music Gen
